{
  "$schema": "https://schemas.tauri.app/config/2/capability.json",
  "identifier": "default",
  "description": "Default capability for the main MoonSight window (appData SaveStore commands).",
  "windows": ["main"],
  "permissions": ["core:default", "allow-moonsight-save"]
}
